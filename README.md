# ai7-human-pattern-neural-network
"A two-layer neural network with backpropagation built in pure Python (no NumPy, no PyTorch) — trained on human number-picking sequences to predict behavioural patterns."
